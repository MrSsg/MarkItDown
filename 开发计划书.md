# MarkItDownDesk 开发计划书

**版本：** 2026-08-01
**目标平台：** Windows x64、Python 3.12、CPU、支持 AVX
**OCR 策略：** 仅使用本地 PaddleOCR；不引入云端视觉模型、API Key、LLM OCR 或 Office 内嵌图片 OCR。

## 1. 当前基线

已完成：

- PyInstaller 发行构建恢复 `requests` 与 `magika` 数据；真实 EXE 已通过 PDF、DOCX、XLSX、JPG 转换回归。
- 单任务队列、取消后续项、普通转换超时等待、会话磁盘缓存、文件/PDF/ZIP 默认限制。
- 图片与低文本 PDF 页的离线 OCR 接入、JSONL 协议、SHA-256 与 Ed25519 清单校验、离线 ZIP 导入、健康检查与回滚机制。
- 冻结版内核自更新已移除；界面已改为双栏工作台，主题已统一并完成主题切换按钮动画。

本计划只覆盖尚未完成、尚未通过真实验收或需要发布方配置的事项。

## 2. 范围与约束

### 纳入范围

- 发布链路、CI、OCR 签名与真实验收。
- 批处理进度、失败日志、资源治理与结果生命周期。
- OCR 结果正确性、性能与可观测性。
- 工作台可访问性、窄窗口适配与主题收尾。

### 不纳入范围

- 云端 OCR、GPU 推理、API Key 管理。
- Office 文档内嵌图片 OCR。
- 自动更新 EXE、冻结包内替换 Python 内核。
- ZIP 资源限制以外的大规模文件索引或云端同步。

## 3. 阶段一：发布信任链与 CI 门禁（P0）

### 3.1 配置正式 OCR 签名密钥

**问题：** `markitdown-desktop/app/ocr_public_keys.json` 当前为空，正式桌面版不会信任任何 OCR 发布清单。

**任务：**

1. 在 `ocr-engine/.venv` 离线环境运行 `generate_signing_key.py` 生成 Ed25519 密钥对。
2. 将私钥仅保存为 GitHub Actions Secret：`MARKITDOWN_OCR_SIGNING_KEY`。
3. 将公钥写入 `app/ocr_public_keys.json`，密钥 ID 固定为 `markitdown-ocr-v1`。
4. 使用 `MARKITDOWN_OCR_PUBLIC_KEYS_FILE` 构建正式桌面 EXE，确认公钥已冻结进包。
5. 使用同一私钥构建 OCR ZIP 与签名清单，并发布至 `ocr-engine-v1` tag 的 GitHub Release。

**验收：**

- 新 EXE 可验证正式清单并安装 OCR 组件。
- 篡改 ZIP、篡改清单、未知 `key_id` 均被拒绝。
- 私钥不出现在仓库、构建产物、日志或文档中。

### 3.2 完善 GitHub Actions 门禁

**任务：**

1. 在 `desktop-regression` Job 中增加 `python -m unittest discover -s markitdown-desktop/tests`。
2. 将 `contents: write` 权限只授予 OCR 发布 Job；普通 push/PR 使用只读权限。
3. tag 发布时依次执行：OCR 构建、归档健康检查、签名校验、上传 Release ZIP 与清单。
4. 记录发行构建使用的桌面版本、OCR 版本、SHA-256 与密钥 ID。

**验收：**

- push/PR 任一桌面单元测试、EXE 四格式回归失败均阻止合并。
- tag 可从零产出已签名 OCR Release 资产。
- PR 不拥有发布仓库资产的写权限。

### 3.3 独立环境验收

**任务：** 在无全局 Python、无 Paddle、无开发依赖的 Windows x64 虚拟机或沙箱中运行桌面 EXE。

**验收：** PDF、DOCX、XLSX、JPG 均可转换；OCR 组件安装后可识别中文图片；应用启动与转换不依赖开发机环境变量。

## 4. 阶段二：OCR 协议与识别正确性（P0）

### 4.1 补全 JSONL 协议

**任务：**

1. 引擎从解析请求开始保存 `request_id`；所有 `progress`、`result`、`error` 事件均返回该值。
2. 定义稳定错误码：`ENGINE_START_FAILED`、`INPUT_NOT_FOUND`、`UNSUPPORTED_FILE_TYPE`、`OCR_TIMEOUT`、`MODEL_UNAVAILABLE`、`ENGINE_RUNTIME_ERROR`。
3. 桌面端将错误码、诊断文本、文件路径、阶段和时间写入本地 Job 日志。
4. 组件安装 UI 显示可理解的错误原因，不只显示异常字符串。

**验收：** 单个任务失败时可依据 `request_id` 定位完整日志；批量任务不会混淆不同文件的 OCR 进度或错误。

### 4.2 保留 PDF 原有转换结果

**问题：** 当前 PDF OCR 转换器自行拼接 `pdfplumber` 文本，不能严格保证保留 MarkItDown 的表格、版式和其他结果。

**任务：**

1. 先获取 MarkItDown 原始 PDF Markdown 结果与页级映射。
2. 仅对有效字符少于 32 的页面执行 OCR。
3. 用 OCR 结果补充或替换对应扫描页，其他页面保持原有 Markdown、表格与顺序。
4. 明确加密、损坏、无页码映射 PDF 的降级策略。

**验收：** 混合 PDF 的原生表格/文本不变化，扫描页出现可复制 OCR 文本，且原生文字页不触发 OCR。

### 4.3 建立真实 OCR 验收样本

**任务：** 建立可公开分发或自行授权的测试夹具：中文图片、英文图片、纯扫描 PDF、混合 PDF、低清/旋转图片。

**验收：**

- 中文图片结果包含预期固定文字。
- 扫描 PDF 页数、顺序与文本正确。
- 混合 PDF 只调用指定扫描页。
- 断网、签名错误、哈希错误、损坏组件、离线导入、回滚全部自动化覆盖。

## 5. 阶段三：批处理、日志与资源治理（P1）

### 5.1 统一可量化进度

**任务：**

1. `JobController` 维护批次总项、完成项、当前项页数和当前页。
2. ZIP 预检、PDF 页处理、OCR 页处理转换为统一进度事件。
3. 进度条显示百分比、当前文件、页码与剩余任务数；未知进度时明确显示“准备中”。

**验收：** 100 项批处理和多页 PDF 不再长期显示不确定进度；取消、超时和失败时进度状态一致。

### 5.2 本地 Job 日志与失败恢复

**任务：**

1. 在 `%LOCALAPPDATA%\MarkItDownDesk\sessions\<session-id>\logs` 写入每项 JSONL/文本日志。
2. 失败详情页增加“复制完整日志”“打开日志目录”“重试失败项”。
3. 批次完成仅显示摘要；日志保留到会话清理或用户导出。

**验收：** 批量失败不弹出逐项阻塞窗口；用户能复制并定位任意失败项的完整诊断。

### 5.3 缓存、历史与 ZIP 防护收尾

**任务：**

1. 增加“固定结果”动作；固定或导出时记录导出路径、时间和状态，但不保存 Markdown 全文。
2. 启动时迁移旧 `history.json`，删除遗留 `output_preview` 内容。
3. 对嵌套 ZIP 明确策略：默认拒绝嵌套归档，或在递归预检中累计条目、体积和压缩率。
4. 对 100 项、超限 PDF、超限 ZIP、ZIP 炸弹做压力与内存测试。

**验收：** 退出后未固定会话被清理；历史无正文泄露；超限或嵌套 ZIP 不触发大量解压和内存增长。

## 6. 阶段四：OCR 组件性能与体积（P1）

**现状：** 当前 OCR ZIP 约 409 MiB，且每个 OCR 文件启动一次引擎并初始化 PaddleOCR。

**任务：**

1. 分析模型、Paddle 运行时、PyInstaller 二进制各自体积。
2. 评估更小的中英文 CPU 模型，保持离线识别准确度作为硬门槛。
3. 设计可复用的 OCR 引擎生命周期：同一 Job 内常驻进程、多请求 JSONL、完成后安全退出。
4. 处理子进程 stderr，避免日志管道积压导致死锁。
5. 记录首张图片耗时、后续图片耗时、峰值内存和组件大小。

**验收：** 不降低中文样本通过率；批量 OCR 后续文件耗时明显低于首次；超时仍只终止 OCR 子进程。

## 7. 阶段五：工作台体验与主题收尾（P2）

### 7.1 设置与信息架构

**任务：** 设置页显示桌面版本、已安装 OCR 版本、许可证入口、发布页链接和组件安装状态。

### 7.2 可访问性与响应式验收

**任务：**

1. 为队列、预览、失败详情、主题按钮、历史抽屉定义 Tab 顺序和快捷键。
2. 在窄窗口、125%/150% 缩放下检查双栏最小宽度、长文件名省略、按钮不重叠。
3. 检查明暗主题下文本、状态色和禁用态的对比度。
4. 将 QSS 中遗留固定颜色收敛到 `ThemePalette` 令牌。

**验收：** 键盘可完成导入、开始、取消、重试、复制、导出；所有目标分辨率下界面可用且主题一致。

## 8. 测试矩阵

| 类别 | 必测项目 |
| --- | --- |
| 单元测试 | Job 状态机、取消、超时等待、失败重试、预检限制、缓存清理、签名、哈希、回滚、JSONL 错误关联 |
| 集成测试 | 中文图片、扫描/混合 PDF、ZIP 限制、离线导入、断网安装、损坏恢复 |
| 发行测试 | EXE 启动、PDF/DOCX/XLSX/JPG 转换、无全局 Python 环境、OCR 组件安装与中文复制 |
| 压力测试 | 100 项批次、500 页 PDF、超限 ZIP、取消与超时、峰值内存与响应时间 |
| UI 测试 | 明暗主题、窄窗口、缩放、长文件名、键盘导航、状态对比度 |

## 9. 推荐执行顺序

1. 配置正式密钥和 Release，补齐 CI 单元测试与最小权限。
2. 修复 OCR 错误事件 `request_id`、稳定错误码与本地 Job 日志。
3. 完成中文图片、扫描 PDF、混合 PDF、离线安装/回滚的真实验收。
4. 改进 PDF 原结果保留和批处理可量化进度。
5. 完成缓存/历史迁移、嵌套 ZIP 策略与压力测试。
6. 再优化 OCR 组件体积、引擎复用和工作台体验。

## 10. 关键决策点

- **发布密钥：** 需由发布者创建并保管；没有私钥和正式公钥，OCR 只能进行开发测试，不能对外发布。
- **嵌套 ZIP：** 推荐首版默认拒绝，后续确有业务需求再实现受限递归处理。
- **模型优化：** 以中文准确度和离线稳定性优先，组件体积优化不得破坏验收样本。
- **常驻 OCR 引擎：** 建议限定在单个 Job 生命周期内，避免跨会话进程残留和取消语义复杂化。
